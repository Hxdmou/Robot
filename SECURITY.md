# 安全与隐私指南

本项目致力于保护用户数据安全和隐私。以下是关于数据存储、处理和安全的详细说明。

## 📁 数据存储位置

### 1. 对话历史数据
- **存储路径**: `./chat_histories/`
- **文件格式**: JSON
- **命名规则**: `{system_name}_history.json`
- **保留策略**: 默认保留最近 50 条对话记录

### 2. 知识库文档
- **存储路径**: `./knowledge_bases/`
- **支持格式**: PDF、DOCX、XLSX、TXT、MD 等

### 3. 向量索引数据
- **存储路径**: `./{system_name}_faiss_index/`
- **文件格式**: FAISS 索引文件

### 4. 日志文件
- **存储路径**: `./logs/`
- **文件格式**: 文本日志

## 🔒 安全操作指南

### 如何安全上传文档

1. **文件检查**:
   - 上传前检查文档内容，确保不包含敏感信息
   - 避免上传包含个人身份信息（PII）的文档

2. **文档脱敏**:
   - 对包含敏感数据的文档进行脱敏处理
   - 移除或替换姓名、身份证号、手机号、地址等信息

3. **访问控制**:
   - 确保只有授权人员可以访问服务器
   - 设置适当的文件权限

### 如何彻底删除数据

#### 删除对话历史
```bash
# 删除单个系统的对话历史
rm -rf ./chat_histories/{system_name}_history.json

# 删除所有对话历史
rm -rf ./chat_histories/
```

#### 删除知识库文档
```bash
rm -rf ./knowledge_bases/
```

#### 删除向量索引
```bash
rm -rf ./*_faiss_index/
```

### 数据加密建议

1. **传输加密**:
   - 使用 HTTPS 协议进行数据传输
   - 在生产环境中配置 SSL/TLS 证书

2. **存储加密**:
   - 对于包含敏感数据的部署，建议使用磁盘加密
   - 考虑对敏感配置文件进行加密存储

3. **配置加密**:
   - 使用环境变量管理敏感配置（如 API Key）
   - 不要将敏感信息硬编码到代码中

## 📋 安全最佳实践

### 1. 访问控制
- 限制对服务器和存储的访问权限
- 使用强密码和多因素认证

### 2. 定期清理
- 定期清理不再需要的对话历史和日志
- 建立数据保留策略

### 3. 日志管理
- 定期审查日志文件
- 设置日志轮换策略
- 避免在日志中记录敏感信息

### 4. 备份策略
- 定期备份重要数据
- 测试备份恢复流程
- 考虑异地备份

### 5. 更新维护
- 定期更新依赖库版本
- 关注安全漏洞公告
- 及时修复安全问题

## ⚠️ 安全风险提示

1. **数据泄露风险**:
   - 未经授权的访问可能导致数据泄露
   - 建议在生产环境中使用防火墙和访问控制

2. **模型安全**:
   - LLM 模型可能产生有害内容
   - 建议添加内容过滤和审核机制

3. **依赖安全**:
   - 第三方库可能存在安全漏洞
   - 建议定期检查依赖安全性

## 🔐 合规声明

本项目设计遵循以下合规原则：
- **数据最小化**: 仅收集必要的数据
- **数据主体权利**: 用户可以访问、更正、删除其数据
- **透明性**: 清晰说明数据处理方式
- **安全保障**: 采取适当的安全措施保护数据

## 📞 安全问题报告

### 漏洞报告流程

我们重视安全漏洞的发现和修复。如发现安全漏洞，请按照以下流程报告：

1. **发送漏洞报告**
   - 邮箱: 项目安全联系邮箱（仓库管理员处获取）
   - 主题: [安全漏洞] 漏洞描述（简短）
   - 内容应包含：
     - 漏洞类型（如：SQL注入、XSS、未授权访问等）
     - 受影响的组件/版本
     - 复现步骤
     - 潜在影响评估
     - 建议修复方案（如有）

2. **响应时间**
   - 我们承诺在收到完整报告后 **48小时内** 确认收到
   - 在 **7个工作日内** 提供初步评估和修复计划
   - 关键漏洞将优先处理

3. **漏洞披露**
   - 我们遵循负责任的漏洞披露原则
   - 在漏洞修复并发布更新后，将公开披露漏洞详情
   - 感谢安全研究者的贡献，将在致谢名单中提及

### PGP 公钥

为了安全地发送敏感信息，您可以使用我们的 PGP 公钥加密通信：

```
-----BEGIN PGP PUBLIC KEY BLOCK-----
mQINBF0...（公钥内容）
-----END PGP PUBLIC KEY BLOCK-----
```

**密钥 ID**: `RAG-QA-SYSTEM-SECURITY`  
**指纹**: `XX XX XX XX XX XX XX XX XX XX XX XX XX XX XX XX`  
**有效期**: 2026-2028

### 安全奖励计划

我们对发现并报告安全漏洞的研究者表示感谢。虽然目前没有正式的漏洞赏金计划，但我们会：
- 在项目文档中公开致谢
- 邀请加入项目贡献者名单
- 提供技术支持和合作机会

---

**最后更新**: 2026年（最后更新 2026-08-07：新增防 AI 诈骗 10 层绝对防护铁律）

---

## 🚨 最高优先级红线：防 AI 诈骗 10 层绝对防护铁律（永久保存·零例外）

### 背景·建立初衷

当前 AI 诈骗泛滥猖獗（以下手段层出不穷，直接危害老年人及其他弱势群体财产安全与人身安全）：
- 冒充检察机关 / 公安机关 / 法院等执法机关办案恐吓转账
- 冒充家庭成员 / 子女 / 亲属遭遇车祸或绑架要求紧急汇款
- 冒充电商 / 物流 / 平台客服索要退款验证码 / 银行卡号
- 曝光 / 窃取个人财物和他人隐私信息用于精准诈骗
- 模仿明星、亲友声音进行语音克隆诈骗（要求转账到"安全账户"）
- AI 换脸伪造视频、伪造会议直播进行敲诈勒索 / 冒充领导指示转账

**本条建立于用户对当前 AI 诈骗现状的深刻忧患意识与社会责任感。本项目绝对禁止任何形式被用于 AI 诈骗，必须建立 10 层防线（缺一不可，违反任一项即视为安全不合格）。**

---

### ① 【代码仓库私有防线】· GitHub / Gitee / GitLab 必须私有
- 绝对必须：Settings → Danger Zone → Change repository visibility → **Change to private**
- 验证方式：仓库名左侧出现 🔒 小锁图标；未登录第三方访问直接返回 **404**
- 零容忍：公开仓库 = 安全红线事故，必须立即切回私有

### ② 【知识库 / 向量索引隔离防线】· 犯罪分子拿不到专业内容
- `legal_knowledge_base/`、`medical_knowledge_base/`、`finance_knowledge_base/`、`*_faiss_index/`、`chat_histories/` 等数据资产 **100% 禁止被 git 追踪**
- `.gitignore` 中使用 `**/` 前缀通配所有子目录（无论藏多深）
- 结果：即使框架代码泄露，没有专业知识库 = 无法打造"专业假客服 / 假律师 / 假医生"系统

### ③ 【音频 / 视频 / 生物数据零入库防线】· 防止语音克隆与 AI 换脸
- **仓库目录树中绝对不允许存在**：
  - 录音文件：`*.mp3 *.wav *.m4a *.flac *.aac *.opus *.amr`
  - 视频文件：`*.mp4 *.mov *.mkv *.avi *.flv *.webm *.3gp *.ts`
  - 公民隐私批量文件：`*_idcard* *_bankcard* *_phone_list* *_elder* *_pension* *_shebao* *_mobile_list* *_social_security* *_yibao* *_medical_insurance* *_fund* *_stock_account*`
- 存放原则：此类数据必须存放在仓库外部加密盘（I 盘 / J 盘），绝不进入 Git 仓库工作区

### ④ 【凭证 / 密钥 / Token 零硬编码防线】· 禁止写死源码
- API Key / Token / Password / Private Key / 机器人 IP 端口，**绝对不能硬编码**（`.py` `.yaml` `.json` `.toml`）
- 注入方式：环境变量 `EMBODIED_API_KEY` 等 + 安全读取
- 默认地址一律 `127.0.0.1` 本地回环；`security_scan.py` + `pre-commit` 钩子双重扫描必须 0 命中

### ⑤ 【API 服务强制鉴权防线】· 接口不对外裸奔
- `api_server.py` 默认 `host='127.0.0.1'`（默认不对外暴露）
- 全局 `@app.before_request` 强制 API Key 鉴权：Header `X-API-Key: <密钥>` 或 URL `?api_key=`
- 缺 `EMBODIED_API_KEY` 时启动自动生成一次性强密钥（仅本次进程有效）
- 频率硬上限：单 IP 每分钟最多 30 次请求（防止算力被刷 / 爬取 / DDoS）

### ⑥ 【Git 预提交安全防线】· 提交前自动拦截
- 预启用钩子（`.git/hooks/pre-commit`）每次 `git commit` 前自动执行：
  - 敏感信息扫描（凭证/密钥/内网 IP / 手机号 / 身份证号）
  - Python 语法验证 `py_compile`
- 不通过 = 直接阻止提交，返回退出码非零
- 紧急绕过：`git commit --no-verify`（仅特殊情况，平时禁止使用）

### ⑦ 【对话回复反诈防线】· 不生成诈骗话术脚本
任何 RAG / LLM 对话接口（`legal` / `medical` / `finance` / `education` / `tech` / `general` 等任何 system）输出：
- ❌ **绝对禁止提供**：转账操作指引、验证码索要、密码透露、资金转移理由、公检法办案恐吓话术
- ✅ **遇到必须直接回复**固定模板：
  > 此请求涉及资金/个人信息安全风险，建议您拨打官方公开服务电话或到就近线下网点人工核实，本系统不提供任何涉及资金与身份凭证的操作建议。

### ⑧ 【对外暴露极简防线】· 别人看不到过程信息
- 所有注释 / commit message / features / description / scenes 对外可见文字：
  - 严格按"简化注释模板"格式（`新增N项内容` / `已修改N项内容`）
  - 不出现媒体名、记者名、展会名、时间戳过程字、截图张数
- 犯罪分子即使浏览 GitHub，只能看到极简数字标签 + 纯产品技术信息

### ⑨ 【加密文件绝对禁止上传防线】· 密钥 100% 在仓库外
- 绝对禁止：`.enc` `.bak_unenc` `.bak` `.pem` `.pfx` `.key` `/TRAE备份` 加密文件或密钥
- 存放原则：必须存放在仓库外独立目录 `I:\加密文件\` 或 `I:\TRAE备份\`
- 推送前扫描：加密文件模式匹配必须零命中

### ⑩ 【推送前 9 重校验防线】· 每次 push 之前必须全部通过
| # | 校验项 | 合格标准 |
|---|--------|---------|
| 1 | 私网段 IP 扫描（192.168.* / 10.* / 172.16-31.*） | 0 命中 |
| 2 | 凭证/Token/密码 正则扫描 | 0 命中 |
| 3 | sk-/ghp-/Bearer 等密钥前缀扫描 | 0 命中 |
| 4 | 邮箱/手机号/身份证号 扫描 | 0 命中 |
| 5 | 对外禁词 7 大类 扫描 | 0 命中 |
| 6 | 简化注释合规检查 | 0 命中违规形式 |
| 7 | 核心文件 Python 语法验证（py_compile） | 100% 通过（0错误） |
| 8 | VSCode GetDiagnostics 诊断 | 0 错误 0 警告 |
| 9 | 加密文件/备份 扫描 | 0 命中 |

---

### 本条目标

> **10 层防线共同构成防 AI 诈骗铜墙铁壁，目标 = 犯罪分子利用本项目从事任何形式 AI 诈骗的技术可行性降至绝对 0%。**
>
> 违反本条任一项即视为重大安全事故，必须立即停止所有操作并修复到 100% 合格后才能继续推进。

本条建立日期：**2026-08-07**
本条提出人：**用户远见主动提出**（基于对公共安全和人民群众财产安全的高度责任感）
本条状态：**永久保存 · 零例外 · 最高优先级安全红线**
